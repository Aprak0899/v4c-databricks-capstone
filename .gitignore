# Only these are committed: .github, resources, src, tests, databricks.yml, README.md
# Everything below is local/reference only — do not commit

local/

# Root-level files not to commit
.cursorignore
*.pdf
