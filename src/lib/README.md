# Shared Python modules

Reusable `.py` code used by notebooks or jobs. Import from here (e.g. add `src` to `PYTHONPATH` or use package imports).

- `ingest_json_bronze_autoloader.py` — JSON bronze ingestion helpers
- `json_reference_code.py` — JSON read/reference utilities
